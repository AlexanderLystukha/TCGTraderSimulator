<Window x:Class="PIIIProject.Views.PackMarketWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PIIIProject.Views"
        mc:Ignorable="d"
        Title="PackMarketWindow" Height="510" Width="680" 
        Background="#21386E" 
        Foreground="#FFCB05"
        WindowStartupLocation="CenterScreen"
        Icon="../Images/icon.ico"
        FontFamily="{StaticResource CustomFont}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <!-- Banner -->
        
        <Image Source="../Images/Titles/pack market.png" Height="90"
               HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5" Grid.Row="0"/>

        <Button x:Name="backBtn" Background="Transparent" BorderThickness="0" HorizontalAlignment="left" 
                    Margin="5,0,0,0" Width="45" Click="BackBtn_Click">
            <Image Source="../Images/backarrow.png" Height="50" Grid.Column="0" RenderTransformOrigin="0.8,0.5" />
        </Button>

        <!-- List of packs in the marketplace -->
        <Border Grid.Row="1" BorderBrush="#FFCB05" BorderThickness="5" Margin="30,0,30,0">
            <ListBox x:Name="lbPackMarket" Foreground="snow" Background="#3466AF" PreviewMouseLeftButtonDown="LbPackMarket_Click"
             Width="610" HorizontalAlignment="Center">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <!-- Using a UniformGrid with two columns to wrap the items into two columns -->
                        <UniformGrid Columns="2" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid HorizontalAlignment="Center" Width="280">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>

                            </Grid.ColumnDefinitions>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Image  Height="220" VerticalAlignment="Center" Margin="0,5,0,10" Source="{Binding BoosterImage}"/>

                            <TextBlock Text="Name:" Foreground="#FFCB05" Grid.Row="1"  HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" Foreground="snow"/>
                            </TextBlock>

                            <TextBlock Text="Price:" Foreground="#FFCB05" Grid.Row="2" HorizontalAlignment="Center" Margin="0,5">
                                        <TextBlock Text="{Binding Price}" Foreground="#28cc25"/>
                            </TextBlock>

                            
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>


        <!-- Back Button -->

        <TextBlock Grid.Row="2" Margin="10" VerticalAlignment="Center" 
                   HorizontalAlignment="Center" FontSize="20">Balance: 
            <TextBlock x:Name="balanceCount" Text="0"/>₽</TextBlock>
        
    </Grid>
</Window>
